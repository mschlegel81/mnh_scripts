lightVersionBuiltins->
begin
  local execResult:=mnhInfo['lightVersionPath']
                   .exec(['-cmd','listBuiltin.print'],false);
  assert(execResult[1]=0);
  assert(execResult[0].isStringList(1));
  execResult[0,0].interpret;
end;

main->listBuiltin.minus(lightVersionBuiltins).sort.map(::print);
