//
//(10^[1..20]-1).map((x)->print(x,'=',x.factorize.join('*')));
//
//rangeGenerator(1).map((n)->10^n-1).filter((x)->0=x mod (11*41)).head(1);
//
//11*41;
//
//isPrime(1111111111111111111);
//
//


per(i>2,base>=2)->gcd(i,base)=1 ? rangeGenerator(1).filter((n)->0=(base^n-1) mod i).head(1)[0]
                                : 0;

[3..1000].map((i)->i=>per(i,2));




